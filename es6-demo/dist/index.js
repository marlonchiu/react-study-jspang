'use strict';

var _temp = require('./temp.js');

console.log(_temp.a); /** 
                       *   模块化操作
                       *      export :负责进行模块化，也是模块的输出。
                       *      import : 负责把模块引入，也是模块的引入操作
                       *  */
